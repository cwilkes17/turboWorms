# CLAUDE.md

See **`AGENTS.md`** at the repo root — it applies to every coding agent
working here, Claude included. Kept as a separate one-line file only because
Claude Code looks for this filename specifically; the actual rules live in
one place (`AGENTS.md`) so they can't drift into two different versions.
